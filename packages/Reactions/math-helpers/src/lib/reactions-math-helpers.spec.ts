import { reactionsMathHelpers } from "./reactions-math-helpers";

describe("reactionsMathHelpers", () => {
  it("should work", () => {
    expect(reactionsMathHelpers()).toEqual("reactions-math-helpers");
  });
});
